export default Object.freeze({
    // Database
    databaseReadError: 'An error occurred while reading from the database.',
    databaseWriteError: 'An error occurred while writing to the database.'
});
